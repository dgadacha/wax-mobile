node_modules/
.DS_Store
*.log
library/audio/
library/previews/
library/covers/
library/artists/
library/albums/
library/album-covers/
library/users/
library/library.json
library/playlists.json
library/profiles.json

# Build outputs
dist/
release/
out/
.vite/
# vite-plugin-pwa dev output (regenerated each `vite dev`)
dev-dist/

# Capacitor native projects — regenerated per checkout via `npx cap add`
ios/
android/

# Env (VITE_API_BASE_URL etc.). `.env.example` is the only committed copy.
# NOTE: dot-less variants too (env.local) — a missing dot once leaked a key.
.env
.env.local
.env.*.local
env.local
env.*.local
*.aikey
secret.txt

# Local Claude Code settings (per-machine permission overrides)
.claude/settings.local.json
library/.auth-secret
